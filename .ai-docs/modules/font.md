# font

/**
 * @brief Модуль определения пользовательских символов для отображения на LCD-экранах.
 *
 * Данный модуль содержит массивы байтов, представляющие собой пользовательские символы,
 * предназначенные для вывода на символьных LCD-дисплеях (например, HD44780) с помощью библиотеки LiquidCrystal.
 * В текущей реализации определён символ градуса (°), который может быть использован для отображения температуры.
 *
 * Ключевые структуры данных
 * gradus — массив из 8 байтов, определяющий пользовательский символ градуса (°) в формате 5x8 пикселей
 */

uint8_t gradus[8]
Символ градуса (°), определённый как пользовательский символ 5x8 пикселей.
Аргументы
Отсутствуют
Возвращает
Отсутствует
Исключения
Отсутствуют
